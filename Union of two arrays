Union of two arrays

1.Create a HashSet
2.add array1 and array2 elements in it.
3.HashSet doesn't allow duplicates.

HashSet<Integer>al=new HashSet<Integer>();
        for(int i=0;i<n;i++){
            al.add(a[i]);
        }
        for(int i=0;i<m;i++){
                al.add(b[i]);
        }
        return al.size();

Time Complexity:O(n+m)
Space Complexity:O(n+m)
