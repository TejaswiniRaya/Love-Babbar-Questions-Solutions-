Cyclically rotate an array by one

Input:A[] = {1, 2, 3, 4, 5}
Output:5 1 2 3 4

1.travel from right to left 
2.Swap the elements throughout the loop

 for(int i=n-2;i>=0;i--){
            int temp=arr[i];
            arr[i]=arr[i+1];
            arr[i+1]=temp;
        }

Time Complexity:O(n)
Space Complexity:O(1)


